# Copy to .env and fill in. .env is gitignored — never commit real keys.
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Modal (cloud sandbox for full-scale leaderboard runs)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
